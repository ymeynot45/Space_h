<div class="intro_container">
<!--   <h3>Choose a Deck</h3> -->
<% @decks.each do |d| %>
<a href="/initiate_new_game?id=<%= d.id %>&user_id=<%= current_user.id %>"><div class="deck_of_cards"><%= d.name.gsub(/\_/, " ").capitalize %></a></div>
<% end %>
<br class = "clearFloat">
</div>

